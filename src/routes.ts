interface Routes {
    HOME: string;
    CATALOG: string;
    FAVORITES: string;
 
  }
  
  const routes: Routes = {
    HOME: '/',
    CATALOG: 'catalog',
    FAVORITES: 'favorites',

  };
  
  export default routes;
  